import React from 'react';
import CircularProgress from '@material-ui/core/CircularProgress';

const MyLoader = ({ className }) => <CircularProgress className={className} />;

export default MyLoader;
